# Runtime configuration for Connectivity Monitor
# Copy this file to /logs/config.env (or mount it there) to override defaults.
# TARGETS: comma-separated list of hosts or NAME=HOST entries. Leave empty to use TARGET_HOST.
TARGETS=

# Interval between probe runs (seconds)
INTERVAL_SECONDS=30

# Enable hop insights via mtr (1/true/yes/on)
ENABLE_MTR=0

# mtr tuning options used when ENABLE_MTR is truthy
MTR_CYCLES=1
MTR_MAX_HOPS=32
MTR_TIMEOUT_SECONDS=6
