# Runtime configuration for Connectivity Monitor
# These values are now generated automatically from docker-compose at container
# startup and written to /logs/config.env. You can still use this file as an
# env_file for docker compose if you prefer, but the compose environment block
# is the single source of truth.

# TARGETS: comma-separated list of hosts or NAME=HOST entries. Leave empty to use TARGET_HOST.
TARGETS=

# Interval between probe runs (seconds)
INTERVAL_SECONDS=30

# Enable hop insights via mtr (1/true/yes/on)
ENABLE_MTR=1

# mtr tuning options used when ENABLE_MTR is truthy
MTR_CYCLES=1
MTR_MAX_HOPS=32
MTR_TIMEOUT_SECONDS=6
